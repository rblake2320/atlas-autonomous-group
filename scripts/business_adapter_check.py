import json;from pathlib import Path;R=Path(__file__).resolve().parent.parent;cfg=json.loads((R/'.claude'/'settings.local.json').read_text(encoding='utf-8'));need=['BUSINESS_WORKSPACE_ROOT','BUSINESS_ASSET_ROOT','BUSINESS_HEARTBEAT_SECONDS','BUSINESS_A2A_DIRECTORY','BUSINESS_MCP_SERVERS'];miss=[k for k in need if k not in cfg.get('env',{})];print('PASS' if not miss else 'FAIL');[print('-',m) for m in miss]
